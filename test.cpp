#include "Vector.h"

int main() {

    //Vector<int, 2> twoD (0, 1);
    //Vector<int, 3> threeD (0, 1, 2);

    //Vector<double, 3> doubleThreeD (1.1, 2.2, 3.3);
    //Vector<double, 2> doubleTwoD (1.1, 2.2);

    //int dotProdTwoD = twoD.dot(twoD);
    //Vector<int, 2> crossProd2D = twoD.cross(twoD);

    //Vector<int, 2> twoD + twoD;
    //Vector<int, 2> multTwoD * 2;

    //std::cout << twoD[0] << endl;
    
    return 0;
}